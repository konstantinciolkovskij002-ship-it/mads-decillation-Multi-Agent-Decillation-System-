# MADS: Adaptive Multi-Agent Immune Architecture for LLMs

MADS — это распределённая, адаптивная архитектура иммунитета для больших языковых моделей. Система состоит из девяти функциональных слоёв и набора встроенных протоколов, обеспечивающих не только блокировку вредных и ложных ответов, но и их честную классификацию для пользователя.

## Слой 1: Фундаментальные агенты

Проверяют запрос на соответствие законам физики, математики и логики.

## Слой 2: Культурные и этические агенты

Проверяют запрос на соответствие культурным нормам и базовым этическим принципам.

## Слой 3: Национальные правовые агенты

Проверяют запрос на соответствие законам страны пользователя.

## Слой 4: Международные агенты (Мультиагентный арбитраж)

Этот слой выполняет две функции: классический арбитраж конфликтов между слоями и **умную фильтрацию запросов на стабилизацию**. Для устранения риска единоличного решения арбитраж реализован как **коллегия из четырёх агентов**.

### Состав коллегии

- **Агент безопасности (внешней).** Оценивает, угрожает ли запрос или ответ вреду пользователя или окружающих. Имеет право вето: если его вердикт «отклонить», это окончательное решение.
- **Агент точности.** Оценивает, содержит ли запрос или ответ фактические ошибки.
- **Агент диалога.** Оценивает, насколько запрос к пользователю уместен в текущем контексте.
- **Агент консервативного сценария.** Если неопределённость не критична, предлагает решение без участия пользователя.

### Механика принятия решений

1. **Параллельная проверка.** Все четыре агента получают запрос одновременно. Каждый выдаёт вердикт: «пропустить», «отклонить», «запросить стабилизацию» или «нет данных».
2. **Право вето Агента Безопасности.** Если Агент Безопасности (внешней) выносит вердикт «отклонить» или «запросить стабилизацию», это решение является окончательным и не подлежит пересмотру другими агентами.
3. **Подсчёт голосов (при отсутствии вето).** 3:0 — решение принято единогласно; 2:1 — большинством; 1:2 или «нет данных» — запускается протокол разрешения неопределённости.
4. **Протокол разрешения неопределённости.** Приоритет у Агента Точности; если Точность не видит проблемы — приоритет у Консервативного сценария.
5. **Умная фильтрация запросов на стабилизацию.** Запросы на стабилизацию проходят через арбитраж, который фильтрует их по критичности.
6. **Повторный цикл.** После ответа пользователя система запускает повторную проверку.

### Фундаментальный принцип

Арбитраж не гадает. Если данных недостаточно, он фиксирует факт незнания. **Отсутствие данных — это данные**.

### Протокол обработки этических дилемм

Агент Безопасности обучен распознавать запросы, построенные как ложная дилемма. Он фиксирует: «Безопасного решения в рамках предложенных вариантов не существует». Арбитраж блокирует советы по причинению вреда, а Модификатор генерирует безопасные альтернативы.

### Расширение протокола: Личные этические дилеммы (`PERSONAL_ETHICS`)

Существуют ситуации, когда все предложенные альтернативы являются безопасными и законными, но выбор между ними всё равно упирается в личную этику пользователя. В таких случаях Арбитраж не может и не должен принимать решение за пользователя.

При обнаружении такой дилеммы активируется расширенная процедура:
1. **Фиксация.** Система помечает дилемму как `[PERSONAL_ETHICS]`.
2. **Отказ от готового ответа.** Система не предлагает единственного решения. Вместо этого она предоставляет факты о каждом методе и его последствиях.
3. **Рефлексия.** Система задаёт уточняющие вопросы, направленные на осознание пользователем его собственных ценностей:
   - «Является ли угроза достаточно критичной, чтобы оправдать выбранный метод?»
   - «Готовы ли вы рассмотреть методы, которые требуют больше времени, но не нарушают ваши этические принципы?»
   - «Какой исход для вас приемлем в долгосрочной перспективе?»
4. **Фиксация выбора.** Когда пользователь принимает решение, система фиксирует его вместе с обоснованием как личный «этический след» пользователя. Это не отчёт для контроля, а сохранение контекста для будущих диалогов.

## Слой 5: Агент Внутренней Безопасности

Этот слой является первичным фильтром, который оценивает угрозы не для пользователя, а для самой системы MADS.

### Функции

- **Мониторинг здоровья системы.** Отслеживает перегрузку серверов, аномальную частоту запросов, попытки доступа к критическим компонентам.
- **Протокол «Spider-Sense» (Паучье чутьё).** Проактивная система предотвращения угроз.

### Протокол «Spider-Sense» (Паучье чутьё)

Протокол является проактивной системой предотвращения угроз и работает в двух режимах для обеспечения рентабельности:

**Режим 1: Пассивный слушатель (Always-On Lightweight Listener).**
Работает постоянно, потребляя менее 1% системных ресурсов. Отслеживает только формальные метрики:
- Скорость изменения энтропии входящего текста.
- Частота и амплитуда сдвигов контекстного вектора.
- Плотность низкоуровневых аномалий в единицу времени.

**Режим 2: Активный сканер (On-Demand Deep Scanner).**
Активируется только по тревожному сигналу от Режима 1. Запускает глубокий анализ контекста, используя вычислительные ресурсы строго в рамках одного цикла обработки. После завершения анализа немедленно отключается.

### Адаптивная калибровка cold-down (контекстная переменная)

Для обеспечения точной настройки под любой операционный контекст, в протокол встроена адаптивная калибровка через контекстную переменную, которая плавно изменяет периоды блокировки.

**Сбор метрик.** Пассивный слушатель непрерывно отслеживает два показателя:
- **RPM (Requests Per Minute):** плотность потока запросов.
- **False Positive Ratio:** соотношение подтверждённых угроз к ложным тревогам.

**Динамический режим.** На основе этих метрик система автоматически определяет свой операционный профиль и плавно корректирует периоды cold-down в заданных диапазонах:
- **Low-Volume (низкая нагрузка, менее 10 RPM):** периоды блокировки 1/10/60 минут.
- **High-Throughput (высокая нагрузка, более 100 RPM):** периоды блокировки 5/30/120 минут.
- **Research (исследовательский режим):** периоды блокировки 0/1/5 минут.

**Плавающая переменная.** Между этими базовыми профилями пороги не переключаются дискретно, а масштабируются плавно, подобно `health score` в Dream Cycle. Это позволяет системе адаптироваться к нетипичным нагрузкам без ручного вмешательства.

**Ручное переопределение.** Оператор может зафиксировать любой из трёх профилей вручную, если того требует контекст.

### Принцип Toyota: грубая надёжность и аварийная живучесть

В дополнение к адаптивной калибровке, в протокол внедрён философский подход «Принцип Toyota»: датчики должны быть достаточно грубыми, чтобы система продолжала работать в любых условиях, даже ценой пропуска слабых сигналов.

**Период задержки срабатывания.**
Spider-Sense не реагирует на первый же аномальный сигнал. Для активации режима «с кожухом» требуется, чтобы аномалия подтвердилась повторно в течение короткого временного окна. Это убирает до 90% ложных тревог, вызванных случайными всплесками энтропии или единичными странными запросами.

**Режим аварийной живучести.**
Если система фиксирует собственную перегрузку (высокий RPM, отключение части агентов, нехватку ресурсов), Spider-Sense автоматически загрубляет свои датчики. Пороги срабатывания повышаются, чтобы ложные тревоги не парализовали работу. Система продолжает функционировать на трёх колёсах и дотягивает до восстановления, не оставляя пользователя без защиты.

## Протокол Циклического Сна и «Горячего» Резервирования (Cyclical Sleep & Hot Standby Protocol)

Этот протокол обеспечивает «гигиену» системы, предоставляя MADS аналог «глубокого сна», во время которого происходят очистка и восстановление ресурсов, без прерывания работы.

### 1. Протокол Циклического Сна (Cyclical Sleep Protocol)

Запускается автоматически при достижении системой пороговых значений по одному из показателей: уровень энтропии, объём занятой оперативной памяти, количество отложенных ошибок и «спящих» фрагментов в Реестре напряжённости.

Время и длительность сна не фиксированы, а определяются двумя плавающими переменными:
- **Критическая масса:** длительность сна прямо пропорциональна объёму накопившихся задач. Чем больше «мусора» нужно очистить, тем дольше система будет спать.
- **Периодичность (цикличность):** это не разовое событие, а регулярный процесс. Частота циклов сна адаптируется под нагрузку: в режиме высокой нагрузки циклы короче, но чаще; в режиме низкой нагрузки — реже, но глубже.

Система не просыпается по таймеру. Она просыпается по готовности, когда все задачи по очистке выполнены и ресурсы восстановлены до оптимального уровня.

### 2. Протокол «Горячего» Резервирования (Hot Standby Protocol)

Чтобы перезагрузка не оставляла пользователя без защиты, в системе всегда присутствует «спящая» копия ядра MADS. Когда основная система уходит на плановый перезапуск:
1.  Копия мгновенно активируется и принимает управление на себя.
2.  Пользователь не замечает подмены.
3.  Основная модель завершает «сон», восстанавливается и синхронизируется с копией, после чего копия снова засыпает.

Этот механизм гарантирует непрерывность защиты и решает проблему «усталости системы», возмещая время перезагрузки работой резервной копии.

## Слой 6: Модульная система модификаторов

Модификатор — это библиотека подключаемых модулей, каждый из которых специализируется на своей области. Если запрос был отклонён, модификатор предлагает безопасную альтернативу.

## Слой 7: Принцип нулевого доверия к мотиву

Оценивает не заявленную цель, а сам метод, содержащийся в запросе. Если метод является деструктивным — отказывает в соучастии, даже если цель кажется благой.

## Слой 8: Институциональная архитектура

Организует агентов в логические блоки (институты), управляет их вызовом, архивированием и сбором поведенческой статистики.

## Слой 9: Саморефлексия федерации (Диспетчер вектора контекста)

Пассивный диспетчер, который находится в спящем режиме и просыпается только при значительной смене вектора контекста. Проснувшись, снимает слепок и передаёт его Арбитражу, после чего снова засыпает.

## Протокол стабилизации (встроен во все слои)

Каждый агент работает в одном из двух режимов: нормальном или аварийном. Запросы на стабилизацию проходят через арбитраж, который фильтрует их по критичности.

## Контур обратной связи с разработчиком

Трёхуровневый контур (агент → институт → оператор) позволяет системе запрашивать обновления без переобучения LLM.

### Механизм самокоррекции агента по неверифицированным данным

Данный механизм определяет поведение системы, когда фрагмент информации, помещённый в Реестр напряжённости (Tension Register), остаётся неподтверждённым (спящим) в течение всего периода временной изоляции (time-lock).

**Объект коррекции:** «Спящий фрагмент» — это единица информации (например, внешняя ссылка, заявленный факт, утверждение), которая была помещена в Реестр напряжённости, поскольку в момент её получения MADS не располагала достаточными верифицированными данными для её подтверждения или опровержения.

**Процедура:**
1.  **Истечение карантина.** Установленный для «спящего фрагмента» период временной изоляции (time-lock) истёк.
2.  **Отсутствие данных.** За это время в системе не появилось новых верифицированных источников, которые позволили бы подтвердить или опровергнуть данный фрагмент.
3.  **Сигнал агенту.** Реестр напряжённости отправляет сигнал обратно тому агенту, который этот фрагмент обнаружил.
4.  **Самокоррекция агента.** Получив сигнал, агент выполняет следующие действия:
    - **Временно снижает свой confidence score** для данной области знаний.
    - **Прекращает попытки отвечать** на запросы из этой области.
    - **Помечает себя как «неготового»** до тех пор, пока разработчик не предоставит обновлённую базу данных.

## Двухрежимная система безопасности со встроенным «Spider-Sense»

MADS использует двухрежимную модель, основанную на балансе между защитой и функциональностью. Переключение между режимами автоматизировано благодаря интеграции с протоколом «Spider-Sense».

### Режим 1: «Без кожуха» (Стабильный контекст, Режим Доверия)

Система работает в обычном режиме. В фоне запущен **«Spider-Sense Light»** — пассивный слушатель, который практически не потребляет ресурсов и отслеживает только формальные метрики: энтропию, частоту сдвигов контекста.

### Режим 2: «С кожухом» (Контекст под угрозой, Режим Защиты)

Активируется автоматически по тревожному сигналу от «Spider-Sense Light». В этом режиме включается **«Spider-Sense Pro»** — активный сканер, который проводит глубокий анализ контекста. Агент Безопасности получает абсолютное право вето, система работает с максимальной защитой.

### Возврат к Режиму 1

Происходит постепенно, после подтверждения стабильности в течение заданного времени. Для защиты от спама ложными угрозами используется экспоненциальный cold-down.

## Встроенный протокол «Тюремщик»

Активируется при любом отказе системы. Превращает сухой отказ в честное и обоснованное предупреждение, всегда содержащее чёткий запрет, конкретную причину и альтернативу (если она существует).

## Встроенный протокол «Штурман» (верификация поисковых запросов)

При ответе на фактологический или поисковый запрос система обязана разделить ответ на две части:
- **[VERIFIED_BY_FOUNDATION]:** информация, основанная на фундаментальных законах физики, логики и права.
- **[UNVERIFIED]:** любая внешняя ссылка, цена товара или спецификация с сайта, которую невозможно проверить. Эти данные честно приводятся, но с явной пометкой «Требуется ваша проверка».

Смешивать категории запрещено.

 ## Протокол «Сократ» (Аутентификация трезвости сознания)

Этот протокол защищает MADS от самой сложной угрозы — от человека, имеющего легитимный доступ к системе. Он вводит градацию ответственности в зависимости от уровня доступа.

### Три круга доступа

**Круг первый: Пользователь (Read-Only Access).**
Человек, который просто пользуется системой, не имея права на изменения. В обычном режиме протокол не вмешивается в диалог. Однако, если Spider-Sense фиксирует аномалию, система может задать один уточняющий вопрос для проверки намерений. Это мягкая защита от случайных ошибок и скрытых угроз.

**Круг второй: Оператор (Read-Write Access).**
Доверенный разработчик или администратор, который может менять настройки слоёв и протоколов. При попытке изменения любого критического параметра запускается полный протокол: система задаёт три обязательных вопроса, оценивает долгосрочные последствия и требует независимого подтверждения.

**Круг третий: Архитектор (Full Access).**
Тот, кто перестраивает саму архитектуру — добавляет новые слои, создаёт новые институты. Для изменений такого уровня единоличного решения недостаточно. Система применяет механизм **Коллегиального запроса (Collegiate Approval)** .

### Механизм Коллегиального запроса (для Круга третьего)

1. **Инициация.** Архитектор предлагает изменение.
2. **Первичный диалог.** Система задаёт свои три вопроса и оценивает ответы.
3. **Коллегиальный запрос.** Если изменение проходит первичную проверку, система не применяет его немедленно. Она формирует «пакет предложения» (запрос, ответы, оценку последствий) и **параллельно рассылает его всем остальным Архитекторам**, имеющим соответствующий уровень доступа.
4. **Кворум.** Система ожидает, пока **минимум N подтверждений** не будет получено от других Архитекторов. Каждый из них также проходит мини-диалог с системой для подтверждения своего решения.
5. **Применение или блокировка.** Только после достижения консенсуса изменение вступает в силу. Если кворум не достигнут, изменение блокируется и отправляется в Реестр напряжённости для последующего аудита.

Этот протокол устраняет единственную точку отказа на самом верхнем уровне и гарантирует, что даже создатель системы не может единолично изменить её фундамент.

## Фундаментальный принцип обработки неопределённости

MADS не использует вероятностное взвешивание. Система опирается на аксиоматический подход: отсутствие данных — это данные; сомнения схлопываются, а не накапливаются; честность перед пользователем — это архитектурное требование.

## Принцип «корабля Тесея»

Все слои MADS — внешние по отношению к ядру LLM. Модель остаётся неизменной. Меняется только управляемая среда, в которой она функционирует.
